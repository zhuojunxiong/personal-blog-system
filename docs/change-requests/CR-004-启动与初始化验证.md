# CR-004 启动与初始化验证

## 0. 使用说明

本文用于记录 `work/v1.0-course-delivery` 分支下启动、初始化、登录、页面访问、E2E 执行和浏览器人工验收结果。

本文不修改业务代码，不直接修复问题，不新增功能。本文中的结论只基于本次实际命令输出、自动 HTTP 检查、E2E 结果和项目负责人浏览器人工检查。已执行项必须记录真实结果；未执行项不得写成通过；如果后续验证发现问题，应另开修复 CR 处理，不在本 CR 中直接进入代码修复。

本文不读取或修改 `software-practice-records`。本文继续遵守 ADR-002 方案 B：保留 Flask + Jinja2 + SQLAlchemy + SQLite，在测试保护、需求确认和 ADR 约束下做受控局部修改；本 CR 当前阶段不做局部重构。

## 1. 变更编号

- CR 编号：CR-004
- 状态：执行完成，待提交（已完成依赖安装、数据库初始化、演示数据导入、自动 HTTP 页面检查、E2E 执行、管理员登录、后台页面访问、普通用户登录、个人中心、写作页、我的专栏和后台权限隔离人工验证；核心功能链路可运行，但发现多处视觉一致性和按钮可见性问题，需后续独立视觉修复 CR 处理）

## 2. 变更标题

- 标题：启动与初始化验证

## 3. 变更背景

当前系统理解已完成，下一步必须验证当前项目能否真实启动、初始化、登录和演示。

前置依据：

1. `AGENTS.md` 要求不得伪造测试结果、不得把未实现或未验证内容写成已完成。
2. `docs/change-requests/CR-003-需求澄清与新增功能评估.md` 已将“启动与初始化验证”和“E2E 测试执行记录”分流到 CR-004。
3. `docs/system-understanding/07_当前系统风险与不确定项.md` 将“当前未执行启动、初始化和 E2E，不能证明当前环境可复现”列为高风险 R001。
4. `docs/09_测试报告.md` 中启动、初始化和 E2E 相关用例均为计划或模板，当前执行状态为“待执行”。
5. `docs/功能展示清单.md` 将 F036、F037、F049、F050 标注为需验证或需补证据。

本 CR 的目标是为后续真实执行提供清晰边界和记录模板，不提前写入验证通过结论。

## 4. 涉及需求编号

| 需求编号 | 需求含义 | 本 CR 关系 | 当前状态 |
| --- | --- | --- | --- |
| F036 | 演示数据初始化和默认管理员初始化 | 验证初始化脚本、演示数据、默认账号是否可用 | 已执行：初始化成功，演示数据已生成，默认管理员存在且可登录 |
| F037 | 自动化或端到端测试覆盖主要链路 | 制定并记录 E2E 执行计划和结果模板 | 已执行：`test_e2e.py` 通过 84、失败 0、错误 0 |
| F049 | README 和启动指南指导启动、初始化和配置 AI | 检查 README 启动流程与真实流程是否一致 | 已执行：启动、初始化、演示数据、账号与 AI 配置差异已记录 |
| F050 | 文档链路形成需求、设计、代码、测试追溯 | 将启动验证结果纳入可追溯交付证据 | 已执行：CR-004 已形成启动、数据、页面、登录、E2E 与问题记录 |
| N002 | 当前启动与初始化验收记录 | 形成启动、初始化、默认账号和访问记录 | 已执行 |
| N003 | 当前 E2E 测试执行报告 | 形成 E2E 执行计划和真实执行记录 | 已执行 |

补充说明：本 CR 只证明“当前迁移后的项目可启动、可访问、可登录、可测试”，不代表所有视觉体验均已满足最终交付要求。浏览器人工验收发现的问题已登记在第 16 节，并建议后续独立视觉修复 CR 处理。

## 5. 验证目标

| 目标编号 | 验证目标 | 成功口径 | 当前状态 |
| --- | --- | --- | --- |
| VG-001 | 确认依赖是否满足项目运行 | 依赖安装或依赖检查无阻塞，异常需记录 | 已执行：联网授权后依赖安装成功；`urllib3` / LibreSSL 警告未阻塞 |
| VG-002 | 确认数据库能否安全初始化 | 已备份或使用测试库后，初始化脚本按预期执行 | 已执行：执行前无历史 SQLite 数据库，已记录数据库策略并生成默认库 |
| VG-003 | 确认演示数据能否生成 | 演示数据脚本执行结果、数据规模和账号状态有真实记录 | 已执行：演示用户、文章、分类、标签、专栏、评论、点赞、收藏和 AI 日志计数已记录 |
| VG-004 | 确认默认管理员账号是否可登录 | 使用默认管理员账号登录成功或记录失败原因 | 已执行：`admin / admin123456` 浏览器人工登录成功 |
| VG-005 | 确认项目能否按 README 启动 | 启动命令、监听地址、报错或成功状态有真实记录 | 已执行：本地已有项目服务监听 `127.0.0.1:5000`，自动 HTTP 访问成功；端口占用与沙箱权限问题已记录 |
| VG-006 | 确认核心页面能否访问 | 首页、登录页、注册页、文章页、个人中心、写作页、后台页有访问记录 | 已执行 / 部分通过：核心页面均可访问且数据可见，但公开列表页、后台页和我的专栏页存在视觉一致性问题，注册与后台登录按钮存在默认态可见性问题 |
| VG-007 | 确认 E2E 是否可执行 | 记录执行命令、前置条件、通过/失败/错误数量和结论 | 已执行：`test_e2e.py` 通过 84、失败 0、错误 0 |
| VG-008 | 确认 README 与真实启动流程是否一致 | 差异逐项记录，不直接修改 README | 已执行：真实执行流程与 README 差异已记录，README 修改应后续另开文档校准 CR |

总体结论：CR-004 的功能性验证结果为“核心链路可运行”；展示性验证结果为“存在视觉一致性和按钮可见性问题，需后续修复”。

## 6. 验证环境

| 项目 | 计划值或记录要求 | 当前记录 |
| --- | --- | --- |
| 仓库 | `personal-blog-system` | 已确认 |
| 分支 | `work/v1.0-course-delivery` | 已确认 |
| 提交号 | 执行前记录 `git rev-parse --short HEAD` | 初始化阶段为 `9df5791`，启动/E2E 阶段为 `b71b98d` |
| 操作系统 | 执行时记录实际系统 | macOS 26.4，Build 25E246 |
| Python 版本 | 执行时记录 `python --version` 或 `.venv/bin/python --version` | Python 3.9.6 |
| 虚拟环境 | 优先使用项目 `.venv`，如不存在需记录实际环境 | 初始未发现 `.venv`，本次执行 `python3 -m venv .venv` 后创建 |
| 依赖文件 | `requirements.txt` | 已确认存在，并已执行依赖安装 |
| 数据库类型 | SQLite，继续遵守 ADR-002 方案 B | 已确认 |
| 数据库文件 | `instance/personal_blog.sqlite` 或测试专用 SQLite 文件 | 本次初始化后生成 `instance/personal_blog.sqlite`，约 356K |
| 启动地址 | README 预期为 `http://127.0.0.1:5000/` | 已确认：本轮测试时项目服务可通过 `http://127.0.0.1:5000/` 访问 |
| AI 配置 | 本 CR 不要求真实 AI 调用；如涉及页面展示，只记录是否配置，不记录密钥 | 已确认：后台 AI 状态页可访问，但当前未配置真实 `AI_API_KEY`，不记录 AI 真实调用通过 |

## 7. 数据库备份要求

执行任何会写入或重置数据库的命令前，必须先确认数据库策略。

| 检查项 | 要求 | 当前状态 | 实际记录 |
| --- | --- | --- | --- |
| DB-001 | 确认是否存在 `instance/personal_blog.sqlite` | 已执行 | 执行前 `instance` 目录仅有 `.gitkeep`，未发现 `instance/personal_blog.sqlite` |
| DB-002 | 如使用现有数据库，先备份 SQLite 文件 | 已执行 | 未发现现有 SQLite 数据库，因此无历史数据库文件需要备份 |
| DB-003 | 如使用测试数据库，记录测试数据库路径和配置方式 | 已执行 | 使用默认 SQLite 路径 `instance/personal_blog.sqlite`；执行前无历史数据文件 |
| DB-004 | 执行 `scripts/init_db.py --reset` 前确认会重置数据 | 已执行 | 本次未执行 `--reset`，按当前验证指令执行 `python scripts/init_db.py` |
| DB-005 | 执行 `scripts/demo_data.py` 前确认数据写入范围 | 已执行 | 在初始化后的默认 SQLite 数据库中写入演示数据 |
| DB-006 | 执行 E2E 前确认是否会写入 SQLite | 已执行 | `test_e2e.py` 会使用默认应用数据库创建、修改和清理部分测试数据，不是完全只读测试；该风险已在第 16 节登记 |

不得在未备份或未使用测试库的情况下直接覆盖有价值的本地数据。

### 数据库初始状态记录

- 当前未发现已有稳定用户数据。
- 当前项目进入 CR-004 启动验证时，需要先执行初始化脚本和演示数据脚本。
- 本次不手工伪造用户数据，不直接修改数据库文件。
- 数据生态通过 `scripts/init_db.py` 和 `scripts/demo_data.py` 生成，并在执行后记录实际数量。

## 8. 依赖安装检查

本节创建 CR 时只规划检查项；本次执行已完成依赖安装相关记录，未覆盖的后续检查仍保持“待执行”。

| 检查项 | 计划命令或动作 | 预期记录 | 状态 | 实际结果 |
| --- | --- | --- | --- | --- |
| DEP-001 | 检查 `.venv` 是否存在 | 记录存在或不存在 | 已执行 | 初始不存在；已执行 `python3 -m venv .venv` 创建 |
| DEP-002 | 检查 Python 版本 | 记录实际版本 | 已执行 | `.venv/bin/python --version` 输出 `Python 3.9.6` |
| DEP-003 | 检查 `requirements.txt` 是否存在 | 记录文件是否存在 | 已执行 | 文件存在 |
| DEP-004 | 按需执行 `.venv/bin/pip install -r requirements.txt` | 记录是否安装、是否失败、失败原因 | 已执行 | 首次因沙箱网络 DNS 限制失败；联网授权后安装成功 |
| DEP-005 | 检查 Flask、SQLAlchemy、Flask-Login 等核心依赖 | 记录缺失或满足情况 | 已执行 | Flask 3.1.3、Flask-SQLAlchemy 3.1.1、Flask-Login 0.6.3 可导入 |

如依赖安装失败，只记录失败原因；是否修改依赖文件或环境说明，应后续另开 CR。

## 9. 数据库初始化检查

| 检查项 | 计划命令或动作 | 成功口径 | 状态 | 实际结果 |
| --- | --- | --- | --- | --- |
| INIT-001 | 确认数据库备份或测试库策略 | 有明确数据库策略 | 已执行 | 执行前未发现 `instance/personal_blog.sqlite`，无历史数据库需备份；使用默认 SQLite 路径初始化 |
| INIT-002 | 执行 `.venv/bin/python scripts/init_db.py --reset` | 命令执行完成，无未处理异常 | 已执行 | 按当前验证指令执行 `python scripts/init_db.py`，输出“数据库初始化完成。默认管理员：admin / admin123456” |
| INIT-003 | 检查数据库文件是否生成 | SQLite 文件存在且路径明确 | 已执行 | 已生成 `instance/personal_blog.sqlite` |
| INIT-004 | 检查核心表是否创建 | 用户、文章、分类、标签、专栏、评论、点赞、收藏、AI 日志等表按实际结果记录 | 已执行 | 初始化并导入演示数据后，模型计数见“演示数据执行结果” |
| INIT-005 | 检查默认分类或基础数据 | 只记录真实存在的数据，不预设通过 | 已执行 | `默认分类` 存在；默认管理员 `admin` 存在，role=`admin`，status=`active` |

## 10. 演示数据检查

| 检查项 | 计划命令或动作 | 成功口径 | 状态 | 实际结果 |
| --- | --- | --- | --- | --- |
| DEMO-001 | 执行 `.venv/bin/python scripts/demo_data.py` | 命令执行完成，无未处理异常 | 已执行 | 执行成功，脚本输出“V5.1 演示数据写入完成” |
| DEMO-002 | 检查演示普通用户 | 记录 `alice`、`bob`、`carol` 或实际生成用户是否存在 | 已执行 | `alice`、`bob`、`carol` 均存在，role=`user`，status=`active` |
| DEMO-003 | 检查演示文章 | 记录文章数量和发布状态 | 已执行 | 文章数量 114 |
| DEMO-004 | 检查演示专栏 | 记录专栏数量和状态 | 已执行 | 专栏数量 55 |
| DEMO-005 | 检查分类与标签 | 记录分类、标签数量和代表性数据 | 已执行 | 分类数量 11，标签数量 65；分类包含初始化生成的 `默认分类` |
| DEMO-006 | 检查评论、点赞、收藏等互动数据 | 按实际存在情况记录 | 已执行 | 评论 300，点赞 1179，收藏 556 |

演示数据规模必须以执行后的真实结果为准，不引用脚本存在或 README 描述作为通过证据。

### 演示数据执行结果

执行命令：

```bash
python scripts/init_db.py
python scripts/demo_data.py
```

模型计数命令执行结果：

| 数据类型 | 数量 |
| --- | ---: |
| 用户 | 56 |
| 文章 | 114 |
| 分类 | 11 |
| 标签 | 65 |
| 专栏 | 55 |
| 评论 | 300 |
| 点赞 | 1179 |
| 收藏 | 556 |
| AI 日志 | 1 |

补充说明：分类数量为 11，是因为 `scripts/init_db.py` 先创建 `默认分类`，`scripts/demo_data.py` 再写入 10 个演示分类。

## 11. 默认管理员账号检查

README 当前列出的默认管理员账号为：

```text
admin / admin123456
```

| 检查项 | 检查动作 | 成功口径 | 状态 | 实际结果 |
| --- | --- | --- | --- | --- |
| ADMIN-001 | 初始化后检查管理员用户是否存在 | 数据库中存在管理员用户或记录不存在原因 | 已执行 | `admin` 存在，role=`admin`，status=`active` |
| ADMIN-002 | 打开 `/admin/login` | 后台登录页可访问 | 已执行 | 浏览器人工确认：后台登录页可打开，表单可显示，但登录按钮默认态文字不明显，需后续视觉修复 |
| ADMIN-003 | 使用 `admin / admin123456` 登录 | 登录成功进入后台，或记录失败原因 | 已执行 | 管理员登录成功，进入后台仪表盘 |
| ADMIN-004 | 登录后访问后台仪表盘 | 仪表盘可访问，或记录状态码/错误信息 | 已执行 | `/admin/dashboard` 可访问，文章、用户、专栏、分类、标签、评论等统计数据可显示 |
| ADMIN-005 | 普通用户访问后台 | 普通用户不能进入后台 | 已执行 | 使用普通用户 `alice / user123456` 访问后台管理入口时，被系统拦截并提示“普通用户不能访问后台管理”，权限隔离有效 |

### 后台页面人工确认

| 页面 | 路径 | 人工结果 | 备注 |
| --- | --- | --- | --- |
| 后台仪表盘 | `/admin/dashboard` | 可访问 | 管理员登录后可进入，后台统计数据可显示；但整体视觉风格与前台 v1.0 风格不一致 |
| 用户管理 | `/admin/users` | 可访问 | 用户列表数据可显示；页面风格偏旧，需后续视觉统一 |
| 文章管理 | `/admin/articles/` | 可访问 | 文章列表数据可显示，编辑/删除入口可见；页面风格偏旧，需后续视觉统一 |
| 评论管理 | `/admin/comments` | 可访问 | 评论数据可显示，审核/隐藏/删除入口可见；页面风格偏旧，需后续视觉统一 |
| AI 状态页 | `/admin/ai` | 可访问 | 页面提示 AI 接口已接入但未配置 `AI_API_KEY`；本次仅确认状态页可访问，不代表 AI 真实调用通过 |

## 12. 项目启动命令检查

README 当前启动命令为：

```bash
.venv/bin/python run.py
```

首次运行命令为：

```bash
.venv/bin/pip install -r requirements.txt
.venv/bin/python scripts/init_db.py --reset
.venv/bin/python scripts/demo_data.py
.venv/bin/python run.py
```

| 检查项 | 检查动作 | 记录要求 | 状态 | 实际结果 |
| --- | --- | --- | --- | --- |
| START-001 | 执行启动命令 | 记录命令、开始时间、监听地址和日志摘要 | 已执行 | 实际命令 `.venv/bin/python run.py`；普通沙箱绑定失败，授权后因端口占用未新启服务；详见本轮实际启动记录 |
| START-002 | 检查端口 | 记录是否监听 `127.0.0.1:5000` 或实际地址 | 已执行 | 测试时已有本项目 `run.py` 服务监听 `127.0.0.1:5000`，根路径 HTTP 200 |
| START-003 | 检查启动错误 | 记录 traceback、缺失配置、数据库错误或依赖错误 | 已执行 | 未出现 Python traceback；记录 `Operation not permitted`、`Address already in use` 和非阻塞 `NotOpenSSLWarning` |
| START-004 | 停止服务 | 记录停止方式和是否有异常 | 已执行 | 已停止本项目 `run.py` Python 进程；复查项目 Python 进程不再监听 5000 |

本轮实际启动记录：

| 检查项 | 实际记录 |
| --- | --- |
| 执行日期 | 2026-07-03 |
| 执行分支/提交 | `work/v1.0-course-delivery` / `b71b98d` |
| 实际启动命令 | `.venv/bin/python run.py` |
| 首次启动结果 | Flask 进入启动阶段后，普通沙箱环境绑定端口失败，输出 `Operation not permitted` |
| 授权后启动结果 | 命令再次执行时输出 `Address already in use`，未创建新的监听进程 |
| 监听检查 | `lsof -nP -iTCP:5000 -sTCP:LISTEN` 显示本项目 `run.py` 已有 Python 父子进程监听 `127.0.0.1:5000` |
| HTTP 根路径检查 | 授权执行 `curl http://127.0.0.1:5000/` 返回 `200` |
| 是否监听 `http://127.0.0.1:5000/` | 是，测试时已有本项目 `run.py` 服务监听并可访问；本轮新启动命令因端口占用未另起服务 |
| 是否出现阻塞性 traceback | 未出现 Python traceback；出现阻塞本轮新启动的端口占用提示 `Address already in use` |
| 非阻塞日志摘要 | 启动和 E2E 均出现 `urllib3` / LibreSSL 的 `NotOpenSSLWarning`，未阻塞页面访问或 E2E |
| 停止服务 | 已对本项目 `run.py` 父进程执行 `kill 29952`；复查后项目 Python 进程不再监听 5000，仍有 macOS `ControlCenter` 监听 `*:5000` |

## 13. 主要页面访问检查

以下页面访问检查必须基于实际启动后的服务，不得提前写成通过。

本轮自动检查命令：

```bash
curl -sS -o /tmp/cr004_<path>.html -w '%{http_code}' http://127.0.0.1:5000/<path>
```

本轮自动检查只记录真实 HTTP 状态码；浏览器人工检查记录页面是否能打开、数据是否可见、布局是否明显错乱和按钮是否可用。

| 页面 | 路径 | HTTP 状态码 | 自动结果 | 人工结果 | 日志摘要 |
| --- | --- | ---: | --- | --- | --- |
| 首页 | `/` | 200 | 自动访问成功 | 人工检查：页面可打开，布局整体正常，演示入口可见 | 无 500、404 或重定向 |
| 旧首页 | `/home` | 200 | 自动访问成功 | 人工检查：页面可打开，未发现阻塞性问题 | 无 500、404 或重定向 |
| 发现页 | `/discover` | 200 | 自动访问成功 | 人工检查：可打开，演示数据可见，但页面布局和整体风格明显不一致，需后续视觉修复 | 无 500、404 或重定向 |
| 文章列表 | `/articles` | 200 | 自动访问成功 | 人工检查：可打开，文章数据可见，但页面布局错乱，视觉风格不符合预期 | 无 500、404 或重定向 |
| 分类列表 | `/categories` | 200 | 自动访问成功 | 人工检查：可打开，分类数据可见，但页面布局和风格不一致 | 无 500、404 或重定向 |
| 标签列表 | `/tags` | 200 | 自动访问成功 | 人工检查：可打开，标签数据可见，但页面风格不一致 | 无 500、404 或重定向 |
| 专栏列表 | `/columns` | 200 | 自动访问成功 | 人工检查：可打开，专栏数据可见，但页面布局和风格不一致 | 无 500、404 或重定向 |
| 登录页 | `/login` | 200 | 自动访问成功 | 人工检查：页面可打开，表单可显示，未发现阻塞性问题 | 无 500、404 或重定向 |
| 注册页 | `/register` | 200 | 自动访问成功 | 人工检查：表单可显示，按钮可交互，但注册按钮默认态文字不明显，需要悬停后才清晰可见 | 无 500、404 或重定向 |
| 后台登录页 | `/admin/login` | 200 | 自动访问成功 | 人工检查：表单可显示，按钮可交互，但登录按钮默认态文字不明显，需要悬停后才清晰可见 | 无 500、404 或重定向 |

如实际路径与 README 或展示清单不一致，应记录差异，不在本 CR 中直接修改路由或 README。

### 登录后页面人工确认

| 页面 | 路径 | 账号 | 人工结果 | 备注 |
| --- | --- | --- | --- | --- |
| 普通用户登录 | `/login` | `alice / user123456` | 通过 | 普通用户可正常登录 |
| 个人中心 | `/profile` | `alice / user123456` | 通过 | 登录后可进入个人中心 |
| 写作页 | `/write` | `alice / user123456` | 通过 | 登录后可进入写作页 |
| 我的专栏 | `/my/columns` | `alice / user123456` | 部分通过 | 页面可访问，已有专栏数据可显示，创建专栏入口可见；但页面 UI 和整体风格仍然不统一，需后续视觉修复 |
| 普通用户访问后台 | `/admin/dashboard` | `alice / user123456` | 通过 | 普通用户被系统拦截并提示“普通用户不能访问后台管理”，权限隔离有效 |

## 14. E2E 测试执行计划

本 CR 创建时未运行 E2E。本轮已执行 E2E，并在执行前确认数据库策略和是否需要启动 Flask 服务。

| 项目 | 计划或要求 | 当前状态 |
| --- | --- | --- |
| E2E 脚本 | `test_e2e.py` | 已执行 |
| 是否允许写数据库 | 执行前确认，优先使用测试数据库 | 已确认：脚本使用默认应用数据库，并会创建、修改和清理部分测试数据 |
| 是否需要先初始化数据库 | 执行前确认脚本要求 | 已确认：脚本执行 `db.create_all()`，但依赖演示账号和演示文章数据完成主要链路 |
| 是否需要演示数据 | 执行前确认脚本要求 | 已确认：依赖 `alice`、`bob`、`admin` 和已发布文章等演示数据 |
| 是否需要启动服务 | 执行前确认脚本实现 | 已确认：不需要外部 Flask 服务，脚本使用 Flask `test_client()` |
| AI 测试策略 | 默认不要求真实 AI 调用；如脚本涉及 AI，记录跳过、无 Key 降级或配置状态 | 已确认：只检查写文章页 AI 面板和后台 AI 状态页，不记录真实 AI 调用通过 |

E2E 执行记录模板：

| 执行日期 | 执行人 | 分支/提交 | 数据库策略 | 执行命令 | 通过 | 失败 | 错误 | 结论 |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 2026-07-03 | Codex | `work/v1.0-course-delivery` / `b71b98d` | 使用默认 SQLite `instance/personal_blog.sqlite`；脚本会写入并清理部分测试数据 | `.venv/bin/python test_e2e.py` | 84 | 0 | 0 | 执行通过；无失败详情，无 traceback |

如果 E2E 失败，只记录失败现象、日志摘要和疑似原因；是否修复必须另开修复 CR。

E2E 日志摘要：

- 命令退出码：0。
- 汇总输出：`通过: 84  失败: 0`。
- 错误数量：0；脚本未输出独立错误计数字段，本次按无 traceback、无失败详情记录为 0。
- 非阻塞警告：执行开始时出现 `urllib3` / LibreSSL 的 `NotOpenSSLWarning`，未导致测试失败。

## 15. README 与真实启动流程一致性检查

| 检查项 | README 当前口径 | 真实验证要求 | 状态 | 差异记录 |
| --- | --- | --- | --- | --- |
| DOC-001 | 日常启动：`.venv/bin/python run.py` | 执行后记录是否可启动 | 已执行 | 命令与 README 一致；本轮普通沙箱启动被 `Operation not permitted` 阻塞，授权后因 5000 端口已有本项目 `run.py` 服务占用而未新启服务 |
| DOC-002 | 首次依赖安装：`.venv/bin/pip install -r requirements.txt` | 记录是否足够、是否缺少系统依赖 | 已执行 | 授权联网后依赖安装成功；执行脚本时出现非阻塞 `urllib3` / LibreSSL 版本警告 |
| DOC-003 | 初始化：`.venv/bin/python scripts/init_db.py --reset` | 记录执行结果和数据库影响 | 已执行 | README 写 `--reset`；本次按当前验证指令且因执行前无数据库，实际执行 `python scripts/init_db.py`，成功创建基础库、默认管理员和默认分类 |
| DOC-004 | 演示数据：`.venv/bin/python scripts/demo_data.py` | 记录执行结果和数据规模 | 已执行 | 执行成功；模型计数见“演示数据执行结果” |
| DOC-005 | 访问地址：`http://127.0.0.1:5000/` | 记录实际监听地址和访问状态 | 已执行 | 与 README 一致；授权执行 `curl http://127.0.0.1:5000/` 返回 `200`；浏览器人工确认核心页面可访问 |
| DOC-006 | 管理员账号：`admin / admin123456` | 记录真实登录结果 | 已执行 | 浏览器人工确认管理员可登录后台；后台仪表盘、用户管理、文章管理、评论管理和 AI 状态页可访问 |
| DOC-007 | 普通用户账号：`alice`、`bob`、`carol` / `user123456` | 记录真实登录结果 | 已执行 | 浏览器人工确认 `alice / user123456` 可登录，个人中心、写作页、我的专栏可访问；E2E 中使用 `alice`、`bob` 完成用户与权限链路，未单独验证 `carol` 登录 |
| DOC-008 | AI 配置说明 | 只检查启动流程是否受 AI Key 影响，不记录真实密钥 | 已执行 | 本轮未配置和记录真实 AI Key；后台 AI 状态页可访问并提示未配置 `AI_API_KEY`；页面访问和 E2E 未因缺少 AI Key 阻塞；未把真实 AI 调用写成通过 |

如 README 与真实流程不一致，本 CR 只记录差异；README 修改应进入后续文档校准 CR。

## 16. 发现问题记录表

| 问题编号 | 发现阶段 | 问题描述 | 影响需求 | 严重级别 | 证据 | 建议处理 | 是否另开修复 CR | 当前状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ISSUE-001 | 依赖安装 | 首次执行 `pip install -r requirements.txt` 因沙箱网络 DNS 限制失败，授权联网后成功 | VG-001、F049 | 低 | `Failed to establish a new connection: [Errno 8] nodename nor servname provided` | 本地复现时需保证可访问 PyPI 或提前准备依赖缓存 | 否 | 已记录 |
| ISSUE-002 | 初始化脚本执行 | 执行初始化和数据查询时出现非阻塞 `urllib3` / LibreSSL 版本警告 | VG-001、F049 | 低 | `NotOpenSSLWarning: urllib3 v2 only supports OpenSSL 1.1.1+` | 后续如启动或外部请求异常，再评估 Python/OpenSSL 环境；本次未阻塞初始化和数据导入 | 待确认 | 已记录 |
| ISSUE-003 | 启动验证 | 普通沙箱环境执行 `.venv/bin/python run.py` 时绑定服务被拦截，输出 `Operation not permitted` | VG-005、F049 | 低 | Flask 输出 `Serving Flask app 'app'`、`Debug mode: on` 后出现 `Operation not permitted` | 属于当前执行环境权限限制；本地复现时需允许绑定本机端口 | 否 | 已记录 |
| ISSUE-004 | 启动验证 | 授权后再次执行启动命令时，`127.0.0.1:5000` 已被本项目 `run.py` 服务占用，导致本轮新启动命令未创建新监听进程 | VG-005、F049 | 中 | `Address already in use`；`lsof` 显示本项目 `run.py` Python 父子进程监听 `127.0.0.1:5000` | 后续执行启动验证前先确认并清理旧服务；是否需要独立 CR 取决于是否在团队环境反复出现 | 待确认 | 已记录 |
| ISSUE-005 | E2E 执行 | `test_e2e.py` 会对默认 SQLite 数据库创建、修改和清理测试数据，不是完全只读测试 | VG-007、N003 | 低 | 脚本包含注册 `testuser`、发布/编辑/删除测试文章、创建/删除测试分类和标签等操作 | 后续如需严格隔离，应另行评估测试数据库配置或测试数据隔离 CR | 是 | 已记录 |
| ISSUE-006 | 浏览器人工验收 | `/discover`、`/articles`、`/categories`、`/tags`、`/columns` 均可访问且数据可见，但页面布局和整体视觉风格明显不一致，影响课程答辩展示效果 | F048、F050 | 中 | 浏览器人工截图与页面检查记录 | 后续创建视觉一致性修复 CR，统一公开列表页布局和样式 | 是 | 待处理 |
| ISSUE-007 | 浏览器人工验收 | `/register` 和 `/admin/login` 表单页面可打开，但提交按钮默认态文字不明显，需要悬停后才清晰可见，影响可用性和展示效果 | F048、N003 | 中 | 浏览器人工截图与页面检查记录 | 后续在视觉修复 CR 中统一修复按钮默认态颜色和悬停态 | 是 | 待处理 |
| ISSUE-008 | 浏览器人工验收 | `/admin/dashboard`、`/admin/users`、`/admin/articles/`、`/admin/comments`、`/admin/ai` 均可访问且后台数据可显示，但后台整体视觉风格与前台 v1.0 风格不一致，页面观感偏旧，影响课程答辩展示统一性 | F048、F050 | 中 | 浏览器人工截图与页面检查记录 | 后续创建视觉一致性修复 CR，统一后台页面布局、表格、按钮和整体样式 | 是 | 待处理 |
| ISSUE-009 | 浏览器人工验收 | 普通用户登录后可访问 `/my/columns`，专栏数据和创建入口可显示，但我的专栏页面 UI 与整体 v1.0 风格不一致，表单区域和卡片布局观感较乱，影响用户中心相关页面展示效果 | F048、F050 | 中 | 浏览器人工截图与页面检查记录 | 后续视觉一致性修复 CR 中统一用户中心、我的专栏、创建专栏表单和专栏卡片样式 | 是 | 待处理 |

严重级别建议：

| 级别 | 含义 |
| --- | --- |
| 高 | 阻塞启动、初始化、登录、主页面访问或 E2E 执行 |
| 中 | 不阻塞启动，但影响演示、文档复现、权限或数据可信度 |
| 低 | 不影响主链路，可后续优化或文档说明 |

## 17. 是否允许进入代码修复

| 判断项 | 结论 |
| --- | --- |
| 本 CR 是否允许修改业务代码 | 不允许 |
| 本 CR 是否允许修改 `app/`、`templates/`、`static/`、`models/`、`routes/` | 不允许 |
| 本 CR 是否允许重构 | 不允许 |
| 本 CR 是否允许更换技术栈 | 不允许 |
| 本 CR 是否允许修改数据库结构 | 不允许 |
| 本 CR 是否允许直接修复启动或测试发现的问题 | 不允许 |
| 发现问题后的处理方式 | 记录问题，评估影响，后续另开修复 CR |
| 是否继续遵守 ADR-002 方案 B | 是 |

如验证过程中发现阻塞问题，本 CR 只能记录问题和证据。是否进入修复，必须由项目负责人确认，并创建后续独立 CR。

## 18. 人工确认结论

| 确认项 | 结论 | 确认人 | 日期 | 备注 |
| --- | --- | --- | --- | --- |
| 是否确认本 CR 只做启动与初始化验证，不修改业务代码 | 已确认 | 项目负责人 | 2026-07-03 | 本 CR 仅记录验证结果 |
| 是否确认执行前必须备份数据库或使用测试库 | 已确认 | 项目负责人 | 2026-07-03 | 执行前无历史数据库，已记录数据库策略 |
| 是否确认未执行项统一标注为“待执行” | 已确认 | 项目负责人 | 2026-07-03 | 未执行项未写成通过；未配置 AI Key，未记录 AI 真实调用通过 |
| 是否确认不得伪造验证通过 | 已确认 | 项目负责人 | 2026-07-03 | 仅记录真实命令、E2E 输出和人工浏览器检查 |
| 是否确认发现问题后另开修复 CR | 已确认 | 项目负责人 | 2026-07-03 | 视觉一致性、按钮可见性、后台页面风格和我的专栏 UI 问题后续另开 CR |
| 是否确认不读取或修改 `software-practice-records` | 已确认 | 项目负责人 | 2026-07-03 | 本轮未涉及 |
| 是否确认继续遵守 ADR-002 方案 B | 已确认 | 项目负责人 | 2026-07-03 | 保持 Flask + Jinja2 + SQLAlchemy + SQLite |

当前人工确认结论：已确认。CR-004 已完成依赖安装、数据库初始化、演示数据导入、自动页面访问、E2E 执行、管理员登录、后台页面访问、普通用户登录、个人中心、写作页、我的专栏和后台权限隔离的人工验证。验证结果表明：核心功能链路可运行，演示数据能够显示，E2E 测试已通过；但浏览器人工验收发现公开列表页、后台页面、我的专栏页面存在视觉风格不一致和布局混乱问题，注册页与管理员登录页存在按钮默认态文字可见性问题。上述问题不在 CR-004 中直接修复，后续应通过独立视觉一致性修复 CR 处理。AI 状态页可访问，但当前未配置真实 AI Key，因此不将 AI 真实调用记为通过。
